node_modules
dist

# Scrape debug artifacts (repo root only — not src/modules/scrape/debug/)
/debug/
/raw/

# Local secrets — never commit
config/.env.development
config/.env.staging
config/.env.production