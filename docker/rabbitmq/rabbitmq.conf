# Allow long-running scrape workers without heartbeat disconnects
heartbeat = 0

# Default is 30 minutes — Studies Overseas jobs run for hours while a message is unacked.
# 24h keeps the channel alive through a full catalog scrape.
consumer_timeout = 86400000
