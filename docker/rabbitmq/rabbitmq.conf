# Allow long-running scrape workers without heartbeat disconnects
heartbeat = 0

# Docker port-mapped clients connect from the bridge IP, not 127.0.0.1 inside the container.
loopback_users.guest = false
